require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe ConnectionCategoryMembersController do

  # This should return the minimal set of attributes required to create a valid
  # ConnectionCategoryMember. As you add validations to ConnectionCategoryMember, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) { { "connection_id" => "1" } }

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # ConnectionCategoryMembersController. Be sure to keep this updated too.
  let(:valid_session) { {} }

  describe "GET index" do
    it "assigns all connection_category_members as @connection_category_members" do
      connection_category_member = ConnectionCategoryMember.create! valid_attributes
      get :index, {}, valid_session
      assigns(:connection_category_members).should eq([connection_category_member])
    end
  end

  describe "GET show" do
    it "assigns the requested connection_category_member as @connection_category_member" do
      connection_category_member = ConnectionCategoryMember.create! valid_attributes
      get :show, {:id => connection_category_member.to_param}, valid_session
      assigns(:connection_category_member).should eq(connection_category_member)
    end
  end

  describe "GET new" do
    it "assigns a new connection_category_member as @connection_category_member" do
      get :new, {}, valid_session
      assigns(:connection_category_member).should be_a_new(ConnectionCategoryMember)
    end
  end

  describe "GET edit" do
    it "assigns the requested connection_category_member as @connection_category_member" do
      connection_category_member = ConnectionCategoryMember.create! valid_attributes
      get :edit, {:id => connection_category_member.to_param}, valid_session
      assigns(:connection_category_member).should eq(connection_category_member)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new ConnectionCategoryMember" do
        expect {
          post :create, {:connection_category_member => valid_attributes}, valid_session
        }.to change(ConnectionCategoryMember, :count).by(1)
      end

      it "assigns a newly created connection_category_member as @connection_category_member" do
        post :create, {:connection_category_member => valid_attributes}, valid_session
        assigns(:connection_category_member).should be_a(ConnectionCategoryMember)
        assigns(:connection_category_member).should be_persisted
      end

      it "redirects to the created connection_category_member" do
        post :create, {:connection_category_member => valid_attributes}, valid_session
        response.should redirect_to(ConnectionCategoryMember.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved connection_category_member as @connection_category_member" do
        # Trigger the behavior that occurs when invalid params are submitted
        ConnectionCategoryMember.any_instance.stub(:save).and_return(false)
        post :create, {:connection_category_member => { "connection_id" => "invalid value" }}, valid_session
        assigns(:connection_category_member).should be_a_new(ConnectionCategoryMember)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        ConnectionCategoryMember.any_instance.stub(:save).and_return(false)
        post :create, {:connection_category_member => { "connection_id" => "invalid value" }}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested connection_category_member" do
        connection_category_member = ConnectionCategoryMember.create! valid_attributes
        # Assuming there are no other connection_category_members in the database, this
        # specifies that the ConnectionCategoryMember created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        ConnectionCategoryMember.any_instance.should_receive(:update_attributes).with({ "connection_id" => "1" })
        put :update, {:id => connection_category_member.to_param, :connection_category_member => { "connection_id" => "1" }}, valid_session
      end

      it "assigns the requested connection_category_member as @connection_category_member" do
        connection_category_member = ConnectionCategoryMember.create! valid_attributes
        put :update, {:id => connection_category_member.to_param, :connection_category_member => valid_attributes}, valid_session
        assigns(:connection_category_member).should eq(connection_category_member)
      end

      it "redirects to the connection_category_member" do
        connection_category_member = ConnectionCategoryMember.create! valid_attributes
        put :update, {:id => connection_category_member.to_param, :connection_category_member => valid_attributes}, valid_session
        response.should redirect_to(connection_category_member)
      end
    end

    describe "with invalid params" do
      it "assigns the connection_category_member as @connection_category_member" do
        connection_category_member = ConnectionCategoryMember.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        ConnectionCategoryMember.any_instance.stub(:save).and_return(false)
        put :update, {:id => connection_category_member.to_param, :connection_category_member => { "connection_id" => "invalid value" }}, valid_session
        assigns(:connection_category_member).should eq(connection_category_member)
      end

      it "re-renders the 'edit' template" do
        connection_category_member = ConnectionCategoryMember.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        ConnectionCategoryMember.any_instance.stub(:save).and_return(false)
        put :update, {:id => connection_category_member.to_param, :connection_category_member => { "connection_id" => "invalid value" }}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested connection_category_member" do
      connection_category_member = ConnectionCategoryMember.create! valid_attributes
      expect {
        delete :destroy, {:id => connection_category_member.to_param}, valid_session
      }.to change(ConnectionCategoryMember, :count).by(-1)
    end

    it "redirects to the connection_category_members list" do
      connection_category_member = ConnectionCategoryMember.create! valid_attributes
      delete :destroy, {:id => connection_category_member.to_param}, valid_session
      response.should redirect_to(connection_category_members_url)
    end
  end

end
