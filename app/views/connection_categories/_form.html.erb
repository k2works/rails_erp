<%= form_for(@connection_category) do |f| %>
  <% if @connection_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@connection_category.errors.count, "error") %> prohibited this connection_category from being saved:</h2>

      <ul>
      <% @connection_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :connection_category_class_id %><br />
    <%= f.collection_select :connection_category_class_id, ConnectionCategory.all, :id, :name, {include_blank: true } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
