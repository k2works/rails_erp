<%= form_for(@connection_group) do |f| %>
  <% if @connection_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@connection_group.errors.count, "error") %> prohibited this connection_group from being saved:</h2>

      <ul>
      <% @connection_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
